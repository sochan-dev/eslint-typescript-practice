{
    "compilerOptions": {
        "sourceMap": true,
        "target": "es2021",
        "module": "esnext",
        //今回はeslintを動かすだけでトランスパイル・バンドル・ビルドはしない。
        //"outDir": "dist",
        "moduleResolution": "Node",
        "downlevelIteration": true,
        "lib": ["ES2020", "DOM", "DOM.Iterable"],
        "allowSyntheticDefaultImports": true
    },
    //デフォルトでは.ts .tsx .d.tsが解析される、allowJsがtrueの場合は.js .jsxも解析
    "include": ["./src/**/*"]
}
